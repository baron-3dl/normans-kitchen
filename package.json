{
  "name": "poppas-recipes",
  "version": "1.0.0",
  "private": true,
  "description": "Local dev tooling for Poppa's Recipes (the site itself needs no dependencies)",
  "scripts": {
    "serve": "python3 -m http.server 8765 --directory site",
    "test": "node scripts/test_search.mjs"
  },
  "devDependencies": {
    "playwright": "^1.40.0"
  }
}
